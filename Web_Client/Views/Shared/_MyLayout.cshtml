<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    @Styles.Render("~/Content/css")
</head>
<body>
        <nav class="navbar fixed-top navbar-expand-xl navbar-light bg-light" style="height: 100px;">
            @Html.ActionLink("PilkaBEM 2018", "Index", "Home", new { area = "" }, new { @class = "navbar-brand"})
            <ul class="nav navbar-nav">
                <li class="nav-item"><a class="nav-link" href="#Profil">Profil Kandidat</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li class="nav-item">@Html.ActionLink("Login", "Login", "Login")</li>
            </ul>
        </nav>
    <div style="margin-top: 50px;">
        @RenderBody()
    </div>
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
